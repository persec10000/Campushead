import ActionTypes from '../actions';
const initialState = {

};

export default eventReducer = (state = initialState, action)=>{
    switch(action.type){
        default: 
        return state;
        break;
    }
}